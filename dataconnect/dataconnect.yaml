specVersion: "v1beta"
serviceId: "web-portfolio"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "web-portfolio-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
